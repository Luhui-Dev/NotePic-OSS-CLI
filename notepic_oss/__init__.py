"""notepic-oss: Upload Markdown / MDX / HTML images to Aliyun OSS with compression."""

__version__ = "1.0.0"

# Protocol contract shared with NotePic-OSS-Obsidian — see /PROTOCOL.md at the
# repo root. Bump only per the rules in that file, and bump both sides together.
PROTOCOL_VERSION = "1.0"
